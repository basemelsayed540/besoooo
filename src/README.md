# Source Layout Placeholder

The current app files are built assets, not editable source. Use this `src/`
layout when restoring or rebuilding the React source for maintainable changes.

See `docs/MAINTENANCE_STRUCTURE.md` for the full structure and migration order.

